# Copy to modular_addition/.env (gitignored) and fill in real values.
# Loaded automatically by modular_addition/__init__.py.

# Hugging Face token — read natively by huggingface_hub (push_to_hf.py, train.py).
HF_TOKEN=

# Weights & Biases key — bridged to WANDB_API_KEY in __init__.py so wandb.init() authenticates.
WANDB_TOKEN=
